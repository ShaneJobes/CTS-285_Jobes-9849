To start the app type the following commands into the terminal:

Create virtual environment if one doesn't exist.
python3 -m venv venv

source venv/bin/activate
pip install -r requirements.txt
streamlit run app.py